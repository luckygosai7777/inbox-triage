# Optional. The dev server proxies /api to VITE_API_TARGET, so these are only
# needed if the backend is somewhere other than http://localhost:8000.
VITE_API_TARGET=http://localhost:8000

# Set only when the API is on a different origin in production.
# VITE_API_BASE=https://api.example.com/api
